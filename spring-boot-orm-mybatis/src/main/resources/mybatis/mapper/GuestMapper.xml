<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper01.GuestMapper">
    <select id="getGuestList" resultType="com.renhe.model.Guest">
        select name,position from guest
    </select>
</mapper>
